name: publish-github-packages

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish"
        required: true
        type: string
      channel:
        description: "npm dist-tag"
        required: true
        default: latest
        type: choice
        options:
          - latest
          - beta
          - dev

permissions:
  contents: read
  packages: write

jobs:
  publish:
    if: github.repository == 'crusadesoft/holycode'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./.github/actions/setup-bun

      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          registry-url: "https://npm.pkg.github.com"
          scope: "@crusadesoft"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build SDK
        run: ./packages/sdk/js/script/build.ts
        env:
          HOLYCODE_CHANNEL: ${{ inputs.channel }}
          HOLYCODE_VERSION: ${{ inputs.version }}

      - name: Build CLI
        run: ./packages/holycode/script/build.ts
        env:
          HOLYCODE_CHANNEL: ${{ inputs.channel }}
          HOLYCODE_VERSION: ${{ inputs.version }}
          HOLYCODE_PACKAGE: "@crusadesoft/holycode"
          HOLYCODE_PLUGIN_PACKAGE: "@crusadesoft/plugin"

      - name: Publish SDK
        run: ./packages/sdk/js/script/publish.ts
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_CONFIG_PROVENANCE: false
          HOLYCODE_CHANNEL: ${{ inputs.channel }}
          HOLYCODE_VERSION: ${{ inputs.version }}
          HOLYCODE_PUBLISH_PACKAGE: "@crusadesoft/sdk"
          HOLYCODE_PUBLISH_REGISTRY: "https://npm.pkg.github.com"
          HOLYCODE_PUBLISH_REPO: "https://github.com/${{ github.repository }}"

      - name: Publish plugin
        run: ./packages/plugin/script/publish.ts
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_CONFIG_PROVENANCE: false
          HOLYCODE_CHANNEL: ${{ inputs.channel }}
          HOLYCODE_VERSION: ${{ inputs.version }}
          HOLYCODE_PUBLISH_PACKAGE: "@crusadesoft/plugin"
          HOLYCODE_PUBLISH_REGISTRY: "https://npm.pkg.github.com"
          HOLYCODE_PUBLISH_REPO: "https://github.com/${{ github.repository }}"
          HOLYCODE_PUBLISH_SDK_PACKAGE: "@crusadesoft/sdk"

      - name: Publish CLI
        run: ./packages/holycode/script/publish.ts
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_CONFIG_PROVENANCE: false
          HOLYCODE_CHANNEL: ${{ inputs.channel }}
          HOLYCODE_VERSION: ${{ inputs.version }}
          HOLYCODE_PUBLISH_NPM_ONLY: "1"
          HOLYCODE_PUBLISH_PACKAGE: "@crusadesoft/holycode"
          HOLYCODE_PUBLISH_REGISTRY: "https://npm.pkg.github.com"
          HOLYCODE_PUBLISH_REPO: "https://github.com/${{ github.repository }}"
